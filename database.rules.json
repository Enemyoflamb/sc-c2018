{
  "rules": {
  	"$uid": {
        // grants write access to the owner of this user account
        // whose uid must exactly match the key ($uid)
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
    },
    ".read": false,
    ".write": false
  }
}
